using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using GlobalDataContracts;

namespace VirtualDevice
{
    /// <summary>
    /// The class represents a virtual device for emulating a read NETMF device for communicating with the Gateway.
    /// </summary>
    public class VirtualDevice
    {
        /// <summary>
        /// Default constructor.
        /// </summary>
        public VirtualDevice()
        {
        }

        /// <summary>
        /// The method configures the device using the passed config data.
        /// </summary>
        /// <param name="config"></param>
        public void Configure(DeviceConfig config)
        {
        }

        /// <summary>
        /// The method starts the device.
        /// </summary>
        public void Start()
        {
        }

        /// <summary>
        /// The method stops the device.
        /// </summary>
        public void Stop()
        {
        }
    }
}
